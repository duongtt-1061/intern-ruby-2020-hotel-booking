<% if @message.present? %>
  alert("<%= @message %>");
  $("#p-status-<%= @order.id %>").addClass("bg-<%= @order.status %>");
  $("#p-status-<%= @order.id %>").html("<%= @order.status %>");
  $("#btn-cancel-order-<%= @order.id %>").remove();
<% else %>
  alert("<%= t ".delete_false" %>");
<% end %>
